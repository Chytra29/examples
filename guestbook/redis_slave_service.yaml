apiVersion: v1
kind: Service
metadata:
  name: redis_slave_service
  labels:
    app: redis
	role: slave
	tier: backend
  
spec:
  selector:
    app: redis
	role: slave
	tier: backend
  ports:
    - port: 6379


